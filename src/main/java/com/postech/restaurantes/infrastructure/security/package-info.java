/**
 * Frameworks & Drivers: segurança. Implementações de IPasswordEncoder (BCrypt), ITokenIssuer
 * (JWT) e ISecureTokenGenerator (SecureRandom + SHA-256), o filtro que lê o Bearer e a regra
 * de posse usada nas expressões de autorização. Nada aqui é conhecido pelas camadas de dentro.
 */
package com.postech.restaurantes.infrastructure.security;
